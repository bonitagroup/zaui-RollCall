export const SectionTitle = ({ title }: { title: string }) => (
  <h2
    style={{
      borderLeft: '4px solid #007bff',
      paddingLeft: '8px',
      fontWeight: 'bold',
    }}
  >
    {title}
  </h2>
);
